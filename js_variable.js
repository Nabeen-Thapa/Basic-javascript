let message = "this is js variable e.g";
console.log(message);
let newmessage = "this is new variable message";
console.log(newmessage);    
// print(message)

// variable naming
//  only contain alphabet letters, numbers, and underscores (_)
// The first character of the variable name must not be a number and can't use rteserved words like consol, if
// case sensitive - message and MESSAGE are different

// types 
// cameCase - eg. myNewMessage
// snake case -  eg. my_new_message


// Constant variable
// variable that never change it's value and itn it is declare by "const"

const file_size_limit = 3000
const maxLimit = 200

// var keyword
// declase variable with global scope

if(true){
    var name = "Nabin Thapa";
}
console.log(name);